# play-bin

今後の機能予定


Discord 
=>/operation => Agent //起動・停止など
=>/send =(text)=> Agent //プロセスに直接送って /kill・/give・/whitelist
=>message => Agent　// discordメッセージをMCチャット欄

WebUI
=> Operation => Agent
=> Tail Log => Agent
=> Attach Container => Agent
=> Exec Container => Agent

Agent
=> Start
  => Tail Log
  => Attach Container
  => Exec Container
=> Stop
=> Kill
=> Backup
=> Restore